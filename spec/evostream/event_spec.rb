require "spec_helper"

describe Evostream::Event do
  it "has a version number" do
    expect(Evostream::Event::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
